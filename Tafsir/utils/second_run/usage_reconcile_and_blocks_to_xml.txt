Benutzung

Mismatches korrigieren + Lücken füllen + weiterlaufen (alle DBs):
reconcile_mismatches.py

Nur bestimmte DB(s):
reconcile_mismatches.py --db katheer --db qortoby

Ohne Fortsetzungslauf, nur Reparatur + Gaps:
reconcile_mismatches.py --db katheer --no-resume

Direkter Einzel-Insert (falls man nur nachziehen will):
blocks_to_xml_continue.py --db katheer --exact-id 512