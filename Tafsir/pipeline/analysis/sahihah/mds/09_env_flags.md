# Env Flags (Core)

- `CACHE_DIR` (default: `/app/tools/sahihah/cache`)
- `USE_DUCKDB` (default: `1`)
- `DEBUG` (default: `0`)
- `PLOT_COVERAGE` (default: `0`)
- `ENABLE_STANZA` (default: `0`)
- `ENABLE_SEM_RERANK` (default: `0`)

Tuning:
- `MIN_CANDS`
- `MIN_ACCEPT_SCORE`
- `META_STRENGTH`
- `TOPK_RERANK`
- `ANCHOR_STRONG_K`
- `ANCHOR_TOP_M`
- `ANCHOR_STRIDE`
- `ORDER_PRECHECK_PENALTY`
